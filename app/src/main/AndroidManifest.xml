<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="org.unibl.etf.blbustracker">

<!--    <uses-permission android:name="android.permission.INTERNET" />-->
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
<!--    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />-->
<!--    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> &lt;!&ndash; WRITE_EXTERNAL_STORAGE potrebno za mapu ako je ANDROID < 8.3 &ndash;&gt;-->
<!--    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> &lt;!&ndash; nisam siguran jesu li potrebne ACCESS_WIFI_STATE i ACCESS_NETWORK_STATE(ova se implicitno doda) &ndash;&gt;-->
<!--    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />-->
<!--    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> &lt;!&ndash; requirement for OpenGL ES version 2 &ndash;&gt;-->

    <uses-feature
            android:glEsVersion="0x00020000"
            android:required="true" />

<!--            android:roundIcon="@mipmap/marker_bus_stop_foreground"-->
    <application
            android:configChanges="keyboardHidden"
            android:hardwareAccelerated="true"
            android:icon="@mipmap/marker_bus_stop_foreground"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name="org.unibl.etf.blbustracker.ChoseLanguageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/google_maps_key" />


        <!--
           get your ADs here
       -->
        <meta-data
                android:name="com.google.android.gms.ads.APPLICATION_ID"
                android:value="ca-app-pub-4409229466423092~5314399720"/>

        <activity
                android:name="org.unibl.etf.blbustracker.MapActivity"
                android:configChanges="keyboardHidden|layoutDirection|locale"
                android:hardwareAccelerated="true"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar"
                android:windowSoftInputMode="stateAlwaysHidden" />

    </application>

</manifest>